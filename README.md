## Milk App

### Release

```bash
# intsll release-it tool
npm install --global release-it @release-it/conventional-changelog

# setup github token
export GITHUB_TOKEN={YOUR_GITHUB_TOKEN}

# run release-it tool to create a tag and release page
release-it
```
